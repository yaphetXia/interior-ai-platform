# 家具替换功能使用说明

## 📌 功能概述

家具替换功能允许用户通过**简单的中文关键词输入**，系统自动翻译并组装成**专业的英文提示词**，最终调用 Gemini API 生成高质量的图片。

这个功能基于 **NanoBanana Prompt Framework**，将复杂的提示词工程简化为用户友好的关键词输入。

---

## 🗄️ 数据库设计

### 表结构：`prompt_vocabulary`

| 字段 | 类型 | 说明 |
|------|------|------|
| id | UUID | 主键 |
| category | VARCHAR(50) | 词汇类别 |
| chinese_term | TEXT | 中文词汇 |
| english_term | TEXT | 英文词汇 |
| description | TEXT | 描述（可选） |
| sort_order | INTEGER | 排序 |
| created_at | TIMESTAMP | 创建时间 |
| updated_at | TIMESTAMP | 更新时间 |

### 词汇类别（category）

- `action` - 动作（替换、添加、转换、融入）
- `object` - 对象（椅子、桌子、地毯等）
- `lighting` - 光影（明亮自然光、温暖环境光等）
- `color_style` - 色彩与风格（暖色调、中性色调等）
- `integration` - 效果要求（无缝融合、保持逼真等）
- `camera` - 摄影表现（尼康相机、专业室内摄影等）

### 数据统计

总计 **37 条**词汇数据：
- 动作：4 条
- 对象：13 条
- 光影：5 条
- 色彩与风格：7 条
- 效果要求：4 条
- 摄影表现：4 条

---

## 💻 代码架构

### 1. 数据库层
**文件**: `supabase/migrations/20250102000000_create_prompt_vocabulary.sql`
- 创建 `prompt_vocabulary` 表
- 插入 37 条初始词汇数据
- 配置 RLS 策略（公开只读）
- 添加索引优化查询性能

### 2. 服务层
**文件**: `src/services/promptVocabularyService.js`

**核心函数**：

```javascript
// 解析用户输入的关键词
parseKeywords(input)
// 输入: "替换 椅子 暖色调 自然光"
// 输出: ["替换", "椅子", "暖色调", "自然光"]

// 翻译单个关键词
translateKeyword(chineseKeyword)
// 输入: "椅子"
// 输出: "chair"

// 批量翻译关键词（按类别分组）
translateKeywords(keywords)
// 输入: ["替换", "椅子", "暖色调"]
// 输出: { actions: ["Replace"], objects: ["chair"], colorStyle: ["warm tones"], ... }

// 组装完整提示词
buildFurniturePrompt({ userInput, keywords })
// 输入: { userInput: "替换 椅子 暖色调 自然光", keywords: [...] }
// 输出: "Replace the chair in Figure 2 with the chair from Figure 1. Ensure the new chair perfectly matches the original scene's bright natural daylight, warm tones, maintaining seamless integration."
```

**缓存机制**：
- 词汇表缓存 5 分钟，减少数据库查询
- `clearVocabularyCache()` 可手动清除缓存

### 3. 业务层
**文件**: `src/pages/WorkspacePage.jsx`

**修改点**：
```javascript
// 导入词汇服务
import { parseKeywords, buildFurniturePrompt } from '@/services/promptVocabularyService'

// 在 handleGenerate 函数中
if (selectedPreset === 'furniture') {
  // 解析关键词
  const keywords = parseKeywords(description)

  // 组装专业提示词
  finalPrompt = await buildFurniturePrompt({
    userInput: description,
    keywords: keywords
  })
}
```

---

## 🎯 使用示例

### 示例 1：简单家具替换

**用户输入**：
```
替换 椅子 暖色调 自然光
```

**系统处理**：
1. 解析关键词：`["替换", "椅子", "暖色调", "自然光"]`
2. 查询数据库翻译：
   - 替换 → Replace
   - 椅子 → chair
   - 暖色调 → warm tones
   - 自然光 → bright natural daylight
3. 组装提示词：
   ```
   Replace the chair in Figure 2 with the chair from Figure 1.
   Ensure the new chair perfectly matches the original scene's
   bright natural daylight, warm tones, maintaining seamless integration.
   ```

### 示例 2：复杂场景

**用户输入**：
```
替换 扶手椅 蓝白花 温暖环境光 中性色调 无缝融合 保持透视
```

**组装后的提示词**：
```
Replace the armchair in Figure 2 with the armchair from Figure 1.
Ensure the new armchair perfectly matches the original scene's
warm ambient indoor lighting, neutral palette,
maintaining seamless integration and maintain perspective.
```

---

## 🚀 部署状态

### ✅ 已完成

1. **数据库表创建** - `prompt_vocabulary` 表已创建
2. **数据初始化** - 37 条词汇数据已插入
3. **服务层开发** - `promptVocabularyService.js` 已完成
4. **业务逻辑集成** - 工作台已支持家具替换
5. **数据库迁移** - 已部署到 Supabase

### 🔄 使用流程

1. 用户在工作台选择"家具替换"预设
2. 上传参考图片（图1：源家具，图2：目标场景）
3. 输入中文关键词，例如：`替换 椅子 暖色调 自然光 无缝融合`
4. 系统自动：
   - 解析关键词
   - 查询词汇表翻译
   - 组装专业提示词
   - 调用 Gemini API 生成图片
5. 返回生成的图片

---

## 📊 词汇表查看

可以通过 Supabase Dashboard 查看和管理词汇表：

1. 访问: https://supabase.com/dashboard/project/glnsizwohuzzlsknoxjf/editor
2. 选择 `prompt_vocabulary` 表
3. 可以查看、编辑、添加词汇

---

## 🛠️ 扩展建议

### 添加新词汇

通过 SQL 添加：
```sql
INSERT INTO prompt_vocabulary (category, chinese_term, english_term, sort_order)
VALUES ('object', '沙发', 'sofa', 14);
```

### 添加新类别

可以扩展到更多场景：
- `material` - 材质（木质、金属、布艺）
- `season` - 季节（春夏秋冬）
- `time` - 时间（清晨、黄昏、夜晚）

### 智能匹配

未来可以添加：
- 模糊匹配（"椅"可以匹配"椅子"）
- 同义词支持（"沙发"和"sofa"都能识别）
- AI 辅助扩写（使用 LLM 优化提示词）

---

## ⚠️ 注意事项

1. **词汇表缓存**：更新词汇后需要等待 5 分钟或调用 `clearVocabularyCache()`
2. **关键词分隔符**：支持空格、逗号、顿号、分号
3. **未识别关键词**：如果输入的词不在词汇表中，会被忽略
4. **备用方案**：如果词汇服务失败，会回退到用户原始输入

---

## 📖 参考文档

- **提示词框架**: `/家具替换.md`
- **数据库迁移**: `/supabase/migrations/20250102000000_create_prompt_vocabulary.sql`
- **词汇服务**: `/src/services/promptVocabularyService.js`
- **工作台页面**: `/src/pages/WorkspacePage.jsx`
